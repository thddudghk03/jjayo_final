<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.younghwa_song.jjayo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="짜요짜요"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".QuizResultActivity" android:theme="@style/Theme.Transparent"></activity>
        <activity android:name=".LoadingActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StageStartActivity" />
        <activity android:name=".VideoPlayActivity" />
        <activity android:name=".WordStudyActivity" />
        <activity android:name=".WordQuizActivity" />
        <activity android:name=".QuizStartActivity" />
        <service
            android:name=".MusicService"
            android:enabled="true"
            android:exported="true"></service>
    </application>

</manifest>